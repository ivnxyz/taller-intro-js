/*
  Declaración de una función
*/
function suma(a, b) {
  return a + b
}

// Arrow functions
const resta = (a, b) => a - b

// Llamar a una función
const resultado = suma(123, 340)
console.log(resultado)

console.log(resta(100, 23))